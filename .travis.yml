---
language: node_js
node_js: [ iojs ]
cache:
  directories:
    - bower_components
sudo: false

before_script:
  - npm install -g grunt-cli
  - npm install -g bower
  - npm install
  - bower install

script: grunt
